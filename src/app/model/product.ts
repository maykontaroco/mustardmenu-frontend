export interface Product {
  id: string;
  active: boolean;
  description: string;
  code: string;
  name: string;
  price: number;
  costPrice: number;
  idCategory: string;
  observation: string;
}
